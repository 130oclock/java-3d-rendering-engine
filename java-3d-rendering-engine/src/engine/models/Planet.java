package engine.models;

import java.util.ArrayList;
import java.util.List;

import engine.triangle.Triangle;
import engine.vector.Vector3;

public class Planet {
	
	public Planet() {
		
	}
	
	private Triangle[] generateTerrain(double radius, int resolution) {
		List<Triangle> mesh = new ArrayList<Triangle>();
		
		
		
		return mesh.toArray(new Triangle[0]);
	}
	
	private Triangle[] terrainFace(int resolution, Vector3 localup) {
		
	}
}
